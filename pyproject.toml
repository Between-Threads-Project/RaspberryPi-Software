[project]
name = "raspberrypi-software"
version = "0.1.0"
description = "Software running on the Raspberry Pi to control the 4 motors."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rpi-gpio",
]
